name: Processing_AIP-Alpha7_blocklist
on:
  schedule:
    - cron: "15 0 * * *" # Runs at 00:15 everyday
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use latest node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'latest'
      - run: node write_file_AIP-Alpha7.js 
      - run: node process_AIP-Alpha7-latest.js
      - name: Commit & Push
        uses: actions-x/commit@v6
